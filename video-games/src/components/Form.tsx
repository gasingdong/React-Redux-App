import React from 'react';

const Form = (): React.ReactElement => {
  return (
    <div className="form">
      <form>
        <input type="text" />
        <button type="submit">Search</button>
      </form>
    </div>
  );
};

export default Form;
